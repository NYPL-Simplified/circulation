#!/usr/bin/env python3
"""Refresh availability information for one or more specific books."""

import os
import sys

bin_dir = os.path.split(__file__)[0]
package_dir = os.path.join(bin_dir, "..", "..")
sys.path.append(os.path.abspath(package_dir))

from scripts import AvailabilityRefreshScript       # noqa: E402

AvailabilityRefreshScript().run()
