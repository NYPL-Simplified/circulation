#!/usr/bin/env python3
"""Use the metadata wrangler to create Editions for identifiers that lack them."""

import os
import sys

bin_dir = os.path.split(__file__)[0]
package_dir = os.path.join(bin_dir, "..", "..")
sys.path.append(os.path.abspath(package_dir))

from scripts import CreateWorksForIdentifiersScript     # noqa: E402

CreateWorksForIdentifiersScript().run()
